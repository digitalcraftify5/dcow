# Digital Craftify - Hostinger Next.js Static Export Configuration

AddDefaultCharset UTF-8

# Disable directory listing & allow access to _next
Options -Indexes

<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # 1. ALWAYS ALLOW DIRECT UNTOUCHED ACCESS TO _NEXT STATIC ASSETS
  RewriteRule ^_next/(.*)$ _next/$1 [L]

  # 2. Force HTTPS
  RewriteCond %{HTTPS} off
  RewriteRule ^(.*)$ https://www.digitalcraftify.com/$1 [R=301,L]

  # 3. Force www
  RewriteCond %{HTTP_HOST} ^digitalcraftify\.com$ [NC]
  RewriteRule ^(.*)$ https://www.digitalcraftify.com/$1 [R=301,L]

  # 4. Allow existing files and directories
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]

  # 5. Clean HTML extensionless route fallback (e.g. /about -> /about.html)
  RewriteCond %{DOCUMENT_ROOT}/$1.html -f
  RewriteRule ^(.*)$ $1.html [L]
</IfModule>

# Ensure CSS and JS MIME types are served cleanly
<IfModule mod_mime.c>
  AddType text/css .css
  AddType application/javascript .js
</IfModule>

# Enable GZIP Compression
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/json image/svg+xml
</IfModule>

# Browser Asset Caching
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
</IfModule>
